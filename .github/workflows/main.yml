name: F1 Gas CI Github Actions

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: dckr_pat_H5P0ztzuEbgYlG3vK4xniDepzWE

      - name: Build and push Docker images
        env:
          DOCKER_BUILDKIT: 1
        run: |
          docker-compose build
          docker-compose push

      - name: Deploy
        run: |
          docker-compose up -d
